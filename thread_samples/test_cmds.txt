# Run in the project root
clear && make cli && ./bin/cli -D $(tr '\n' ' ' < thread_samples/threads_pplx.txt)
clear && make cli && ./bin/cli -D $(head -n 5 thread_samples/threads_pplx.txt | tr '\n' ' ')